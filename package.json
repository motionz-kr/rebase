{
  "name": "database-desktop-tool",
  "version": "0.27.1",
  "private": true,
  "packageManager": "pnpm@8.15.9",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm --filter renderer dev & (sleep 2 && pnpm --filter desktop dev)",
    "test": "pnpm --filter desktop test",
    "build:engine": "cd engine && go build -ldflags=\"-s -w\" -o ../apps/desktop/bin/app-engine cmd/app-engine/main.go",
    "build": "pnpm build:engine && pnpm --filter renderer build && pnpm --filter desktop build",
    "package": "pnpm build && pnpm --filter desktop exec electron-builder"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
