# UI 기반 테이블 추가 (Create Table) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 스키마(DB) 노드 우클릭으로 새 테이블을 생성하는 UI를 추가한다 — 컬럼 폼(이름·타입·NULL·기본값·PK·자동증가·UNIQUE) + 편집 가능한 CREATE TABLE SQL 미리보기.

**Architecture:** 프론트엔드 전용. 순수 `buildCreateTable`(방언 인식, TDD)가 폼에서 SQL을 생성하고, 편집 가능한 텍스트 영역에서 사용자가 직접 손볼 수 있으며, 기존 `runDdl`(allowWrite)로 화면의 SQL을 실행한다. 백엔드(Go) 변경 없음.

**Tech Stack:** React 19 + TypeScript, vitest, lucide-react, 기존 Electron `window.electronAPI`.

> **참고(중요):** 이 디렉터리는 git 저장소가 아니다. 각 Task의 "Commit" 스텝은 **git이 초기화된 경우에만** 실행하고 아니면 건너뛴다. 검증은 `npx vitest run` + `npx tsc --noEmit` + `npm run build` + CDP 실제 실행으로 한다.

**도구 경로:** 모든 bash 명령 앞에 `export PATH="$HOME/.nvm/versions/node/v24.14.0/bin:$PATH"`. 테스트/빌드는 `apps/renderer`에서.

---

## File Structure

- **수정** `apps/renderer/src/lib/ddlBuilder.ts` — `CreateColumnSpec` 타입 + `buildCreateTable` 추가.
- **수정** `apps/renderer/src/lib/ddlBuilder.test.ts` — `buildCreateTable` 테스트.
- **신규** `apps/renderer/src/components/CreateTableDialog.tsx` — 폼 + 편집 가능한 SQL + 실행.
- **수정** `apps/renderer/src/components/SchemaExplorer.tsx` — DB 노드 우클릭 메뉴(`dbMenu`) + 다이얼로그 마운트 + 트리 새로고침(기존 `refreshAfterDdl` 재사용).
- **수정** `apps/renderer/src/App.css` — SQL 텍스트영역/생성 컬럼 그리드 스타일.

재사용(변경 없음): `quoteIdent`, `runDdl`, `refreshAfterDdl`, 기존 `TableEditDialog`/`TableActionDialog`.

---

## Task 1: ddlBuilder — buildCreateTable + CreateColumnSpec (TDD)

**Files:**
- Modify: `apps/renderer/src/lib/ddlBuilder.ts`
- Test: `apps/renderer/src/lib/ddlBuilder.test.ts`

- [ ] **Step 1: Write the failing test** (테스트 파일에 추가)

```ts
import { buildCreateTable, type CreateColumnSpec } from './ddlBuilder';

describe('buildCreateTable', () => {
  it('mysql: PK + auto-increment', () => {
    expect(
      buildCreateTable('mysql', 't', [
        { name: 'id', type: 'BIGINT', nullable: false, primaryKey: true, autoIncrement: true },
      ])
    ).toEqual(['CREATE TABLE `t` (\n  `id` BIGINT NOT NULL AUTO_INCREMENT,\n  PRIMARY KEY (`id`)\n)']);
  });

  it('postgres: PK + auto-increment uses IDENTITY', () => {
    expect(
      buildCreateTable('postgres', 't', [
        { name: 'id', type: 'bigint', nullable: false, primaryKey: true, autoIncrement: true },
      ])
    ).toEqual(['CREATE TABLE "t" (\n  "id" bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY,\n  PRIMARY KEY ("id")\n)']);
  });

  it('mysql: unique column + default, no PK → no PRIMARY KEY clause', () => {
    expect(
      buildCreateTable('mysql', 'users', [
        { name: 'email', type: 'VARCHAR(255)', nullable: false, unique: true },
        { name: 'status', type: 'VARCHAR(20)', nullable: true, defaultValue: "'new'" },
      ])
    ).toEqual([
      'CREATE TABLE `users` (\n  `email` VARCHAR(255) NOT NULL UNIQUE,\n  `status` VARCHAR(20) DEFAULT \'new\'\n)',
    ]);
  });

  it('forces NOT NULL on a PK column even if nullable is true', () => {
    expect(
      buildCreateTable('mysql', 't', [{ name: 'id', type: 'INT', nullable: true, primaryKey: true }])
    ).toEqual(['CREATE TABLE `t` (\n  `id` INT NOT NULL,\n  PRIMARY KEY (`id`)\n)']);
  });

  it('composite primary key collects all PK columns', () => {
    expect(
      buildCreateTable('postgres', 'membership', [
        { name: 'user_id', type: 'bigint', nullable: false, primaryKey: true },
        { name: 'group_id', type: 'bigint', nullable: false, primaryKey: true },
      ])
    ).toEqual([
      'CREATE TABLE "membership" (\n  "user_id" bigint NOT NULL,\n  "group_id" bigint NOT NULL,\n  PRIMARY KEY ("user_id", "group_id")\n)',
    ]);
  });

  it('auto-increment column ignores any default value', () => {
    expect(
      buildCreateTable('mysql', 't', [
        { name: 'id', type: 'BIGINT', nullable: false, primaryKey: true, autoIncrement: true, defaultValue: '5' },
      ])
    ).toEqual(['CREATE TABLE `t` (\n  `id` BIGINT NOT NULL AUTO_INCREMENT,\n  PRIMARY KEY (`id`)\n)']);
  });

  it('returns [] when there are no valid columns', () => {
    expect(buildCreateTable('mysql', 't', [])).toEqual([]);
    expect(buildCreateTable('mysql', 't', [{ name: '', type: '', nullable: true }])).toEqual([]);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd apps/renderer && npx vitest run src/lib/ddlBuilder.test.ts`
Expected: FAIL — `buildCreateTable` / `CreateColumnSpec` not exported.

- [ ] **Step 3: Write minimal implementation** (append to `ddlBuilder.ts`)

```ts
export interface CreateColumnSpec {
  name: string;
  type: string;
  nullable: boolean;
  defaultValue?: string;
  primaryKey?: boolean;
  autoIncrement?: boolean;
  unique?: boolean;
}

// Build a CREATE TABLE statement. PK columns are forced NOT NULL; auto-increment
// uses AUTO_INCREMENT (MySQL) or GENERATED BY DEFAULT AS IDENTITY (Postgres) and
// gets no DEFAULT; any PK columns are collected into a table-level PRIMARY KEY.
export function buildCreateTable(driver: Driver, table: string, columns: CreateColumnSpec[]): string[] {
  const cols = columns.filter((c) => c.name.trim() && c.type.trim());
  if (cols.length === 0) return [];
  const q = (n: string) => quoteIdent(driver, n);

  const lines = cols.map((c) => {
    const notNull = !!c.primaryKey || !c.nullable;
    let s = `${q(c.name)} ${c.type.trim()}`;
    if (notNull) s += ' NOT NULL';
    if (c.autoIncrement) s += driver === 'mysql' ? ' AUTO_INCREMENT' : ' GENERATED BY DEFAULT AS IDENTITY';
    if (c.unique) s += ' UNIQUE';
    if (!c.autoIncrement && c.defaultValue && c.defaultValue.trim() !== '') s += ' DEFAULT ' + c.defaultValue.trim();
    return s;
  });

  const pkCols = cols.filter((c) => c.primaryKey).map((c) => q(c.name));
  if (pkCols.length > 0) lines.push(`PRIMARY KEY (${pkCols.join(', ')})`);

  const body = lines.map((l) => '  ' + l).join(',\n');
  return [`CREATE TABLE ${q(table)} (\n${body}\n)`];
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd apps/renderer && npx vitest run src/lib/ddlBuilder.test.ts`
Expected: PASS (전체 ddlBuilder 스위트 green).

- [ ] **Step 5: Commit** (git 초기화 시)

```bash
git add apps/renderer/src/lib/ddlBuilder.ts apps/renderer/src/lib/ddlBuilder.test.ts
git commit -m "feat(ddl): add buildCreateTable builder"
```

---

## Task 2: CreateTableDialog 컴포넌트

**Files:**
- Create: `apps/renderer/src/components/CreateTableDialog.tsx`

- [ ] **Step 1: Implement**

`apps/renderer/src/components/CreateTableDialog.tsx`:

```tsx
import React, { useEffect, useMemo, useState } from 'react';
import { X, Plus, Trash2, AlertTriangle, RotateCcw } from 'lucide-react';
import { buildCreateTable, type CreateColumnSpec, type Driver } from '../lib/ddlBuilder';
import { runDdl } from '../lib/runDdl';

interface Props {
  profileId: string;
  driver: Driver;
  database: string;
  onClose: () => void;
  onApplied: () => void; // success → caller refreshes schema
}

interface Row {
  key: string;
  name: string;
  type: string;
  nullable: boolean;
  defaultValue: string;
  primaryKey: boolean;
  autoIncrement: boolean;
  unique: boolean;
}

let rowSeq = 0;
const newRow = (init?: Partial<Row>): Row => ({
  key: `cr${rowSeq++}`,
  name: '',
  type: '',
  nullable: true,
  defaultValue: '',
  primaryKey: false,
  autoIncrement: false,
  unique: false,
  ...init,
});

export const CreateTableDialog: React.FC<Props> = ({ profileId, driver, database, onClose, onApplied }) => {
  const [tableName, setTableName] = useState('');
  const [rows, setRows] = useState<Row[]>(() => [
    newRow({ name: 'id', type: 'BIGINT', nullable: false, primaryKey: true, autoIncrement: true }),
  ]);
  const [sql, setSql] = useState('');
  const [dirty, setDirty] = useState(false); // user manually edited the SQL
  const [running, setRunning] = useState(false);
  const [error, setError] = useState<string | null>(null);

  const patch = (key: string, p: Partial<Row>) =>
    setRows((prev) => prev.map((r) => (r.key === key ? { ...r, ...p } : r)));

  // The CREATE TABLE statement generated from the form (single statement).
  const generated = useMemo(() => {
    const cols: CreateColumnSpec[] = rows
      .filter((r) => r.name.trim() && r.type.trim())
      .map((r) => ({
        name: r.name.trim(),
        type: r.type.trim(),
        nullable: r.nullable,
        defaultValue: r.defaultValue.trim() || undefined,
        primaryKey: r.primaryKey,
        autoIncrement: r.autoIncrement,
        unique: r.unique,
      }));
    const stmts = tableName.trim() ? buildCreateTable(driver, tableName.trim(), cols) : [];
    return stmts[0] ?? '';
  }, [rows, tableName, driver]);

  // Keep the editable SQL in sync with the form until the user edits it directly.
  useEffect(() => {
    if (!dirty) setSql(generated);
  }, [generated, dirty]);

  const regenerate = () => {
    setDirty(false);
    setSql(generated);
  };

  const canRun = sql.trim() !== '' && !running;

  const apply = async () => {
    setRunning(true);
    setError(null);
    const res = await runDdl(profileId, [sql]);
    setRunning(false);
    if (res.ok) {
      onApplied();
      onClose();
    } else {
      setError(res.error || '실행 실패');
    }
  };

  return (
    <div className="modal-overlay" onClick={onClose}>
      <div className="modal modal-wide" onClick={(e) => e.stopPropagation()}>
        <div className="modal-head">
          <h3>테이블 추가 · <span className="mono">{database}</span></h3>
          <button className="icon-btn" onClick={onClose} title="Close"><X size={15} /></button>
        </div>

        <label className="form-row">
          <span className="form-label">테이블 이름</span>
          <input className="input" autoFocus value={tableName} placeholder="new_table"
            onChange={(e) => setTableName(e.target.value)} />
        </label>

        <div className="ddl-cols">
          <div className="ddl-col-head ddl-col-head-create">
            <span>이름</span><span>타입</span><span>NULL</span><span>기본값</span><span>PK</span><span>AI</span><span>UQ</span><span />
          </div>
          {rows.map((r) => (
            <div key={r.key} className="ddl-col-row ddl-col-row-create">
              <input className="input" value={r.name} placeholder="column" onChange={(e) => patch(r.key, { name: e.target.value })} />
              <input className="input" value={r.type} placeholder="INT / text…" onChange={(e) => patch(r.key, { type: e.target.value })} />
              <input type="checkbox" checked={r.nullable} title="NULL 허용" onChange={(e) => patch(r.key, { nullable: e.target.checked })} />
              <input className="input" value={r.defaultValue} placeholder="(none)" onChange={(e) => patch(r.key, { defaultValue: e.target.value })} />
              <input type="checkbox" checked={r.primaryKey} title="기본키 (PK)" onChange={(e) => patch(r.key, { primaryKey: e.target.checked })} />
              <input type="checkbox" checked={r.autoIncrement} title="자동증가 (AI)" onChange={(e) => patch(r.key, { autoIncrement: e.target.checked })} />
              <input type="checkbox" checked={r.unique} title="UNIQUE (UQ)" onChange={(e) => patch(r.key, { unique: e.target.checked })} />
              <button className="icon-btn" title="행 제거" onClick={() => setRows((prev) => prev.filter((x) => x.key !== r.key))}>
                <Trash2 size={14} />
              </button>
            </div>
          ))}
          <button className="btn btn-secondary btn-xs" onClick={() => setRows((prev) => [...prev, newRow()])}>
            <Plus size={12} /> 컬럼 추가
          </button>
        </div>

        <div className="ddl-preview">
          <div className="ddl-preview-head">
            <span>생성될 SQL{dirty ? ' · 직접 편집됨' : ''}</span>
            <button className="btn btn-secondary btn-xs" onClick={regenerate} disabled={!dirty} title="폼 기준으로 다시 생성">
              <RotateCcw size={12} /> 폼 기준으로 다시 생성
            </button>
          </div>
          <textarea
            className="ddl-sql-edit mono"
            spellCheck={false}
            value={sql}
            onChange={(e) => { setSql(e.target.value); setDirty(true); }}
          />
        </div>

        {error && (
          <div className="alert error"><AlertTriangle size={14} /><span style={{ whiteSpace: 'pre-wrap' }}>{error}</span></div>
        )}

        <div className="modal-foot">
          <button className="btn btn-secondary" onClick={onClose}>취소</button>
          <button className="btn btn-primary" onClick={apply} disabled={!canRun}>
            {running ? <span className="spinner" /> : null} 실행
          </button>
        </div>
      </div>
    </div>
  );
};
```

- [ ] **Step 2: Typecheck**

Run: `cd apps/renderer && npx tsc --noEmit`
Expected: no errors.

- [ ] **Step 3: Commit** (git 초기화 시)

```bash
git add apps/renderer/src/components/CreateTableDialog.tsx
git commit -m "feat(ddl): add CreateTableDialog (form + editable SQL preview)"
```

---

## Task 3: SchemaExplorer DB 컨텍스트 메뉴 + 다이얼로그 마운트 + CSS

**Files:**
- Modify: `apps/renderer/src/components/SchemaExplorer.tsx`
- Modify: `apps/renderer/src/App.css`

먼저 파일을 READ해 현재 구조를 파악한다. 현재 상태:
- import 줄에 `TableEditDialog`, `TableActionDialog` import 존재.
- 상태들: `menu`, `ddl`, `edit`, `tableAction` (34–37행 부근).
- 컨텍스트 메뉴 닫기 effect는 `menu`만 처리(40–52행 부근).
- DB 노드 행: `<div className="tree-row" onClick={() => toggleDatabase(db.name)}>` (210행 부근).
- 테이블 메뉴/다이얼로그 블록(`{menu && ...}`, `{edit && ...}`, `{tableAction && ...}`)이 returned JSX 안에 있음.
- `refreshAfterDdl(dbName)` 헬퍼 존재.

- [ ] **Step 1: import + 상태 추가**

CreateTableDialog import 추가(기존 dialog import 옆):
```tsx
import { CreateTableDialog } from './CreateTableDialog';
```

lucide import에 `FilePlus`를 추가한다(기존 import 줄에 이어서). 예: `..., Type, FilePlus } from 'lucide-react';`

`dbMenu`, `create` 상태 추가(기존 `tableAction` 상태 줄 아래):
```tsx
  const [dbMenu, setDbMenu] = useState<{ x: number; y: number; db: string } | null>(null);
  const [create, setCreate] = useState<{ db: string } | null>(null);
```

- [ ] **Step 2: dbMenu 바깥클릭/Esc 닫기 effect 추가**

기존 `menu` 닫기 effect 바로 아래에 추가:
```tsx
  // Close the database context menu on any outside click or Escape.
  useEffect(() => {
    if (!dbMenu) return;
    const close = () => setDbMenu(null);
    const onKey = (e: KeyboardEvent) => {
      if (e.key === 'Escape') setDbMenu(null);
    };
    window.addEventListener('click', close);
    window.addEventListener('keydown', onKey);
    return () => {
      window.removeEventListener('click', close);
      window.removeEventListener('keydown', onKey);
    };
  }, [dbMenu]);

  const openDbMenu = (e: React.MouseEvent, dbName: string) => {
    e.preventDefault();
    e.stopPropagation();
    setDbMenu({ x: e.clientX, y: e.clientY, db: dbName });
  };
```

- [ ] **Step 3: DB 노드 행에 onContextMenu 추가**

DB 노드 행을 다음으로 바꾼다(기존):
```tsx
          <div className="tree-row" onClick={() => toggleDatabase(db.name)}>
```
교체:
```tsx
          <div className="tree-row" onClick={() => toggleDatabase(db.name)} onContextMenu={(e) => openDbMenu(e, db.name)}>
```

- [ ] **Step 4: DB 메뉴 + CreateTableDialog 마운트**

테이블 메뉴/다이얼로그 블록들과 같은 레벨(returned JSX 안, 예: `{tableAction && (...)}` 다음)에 추가:
```tsx
      {dbMenu && (
        <div className="ctx-menu" style={{ top: dbMenu.y, left: dbMenu.x }} onClick={(e) => e.stopPropagation()}>
          <button className="ctx-item" onClick={() => { setCreate({ db: dbMenu.db }); setDbMenu(null); }}>
            <FilePlus size={13} /> 테이블 추가…
          </button>
        </div>
      )}

      {create && (
        <CreateTableDialog
          key={create.db}
          profileId={profileId}
          driver={driver as 'mysql' | 'postgres'}
          database={create.db}
          onClose={() => setCreate(null)}
          onApplied={() => refreshAfterDdl(create.db)}
        />
      )}
```

- [ ] **Step 5: CSS** — `App.css` 끝에 추가, 그리고 기존 `.ddl-preview-head`를 flex로 만든다.

기존 `.ddl-preview-head` 규칙을 찾아서 다음으로 교체(현재는 `font-size:11px; color:var(--text-3); margin-bottom:4px;` 형태):
```css
.ddl-preview-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  font-size: 11px;
  color: var(--text-3);
  margin-bottom: 4px;
}
```
그리고 파일 끝에 추가:
```css
/* Create-table dialog: 8-column grid (name, type, NULL, default, PK, AI, UQ, remove) */
.ddl-col-head-create,
.ddl-col-row-create {
  grid-template-columns: 1.3fr 1.3fr 40px 1.1fr 30px 30px 30px 30px;
}

.ddl-sql-edit {
  width: 100%;
  min-height: 150px;
  resize: vertical;
  box-sizing: border-box;
  padding: 8px 10px;
  font-size: 12px;
  line-height: 1.5;
  color: var(--text-1);
  background: var(--bg-panel-2);
  border: 1px solid var(--border);
  border-radius: var(--radius);
}
.ddl-sql-edit:focus {
  outline: none;
  border-color: var(--accent-border);
}
```

> 변수 확인: `--text-1`, `--bg-panel-2`, `--border`, `--accent-border`, `--radius`가 App.css에 있는지 grep으로 확인하고, 없는 것은 가장 가까운 기존 토큰으로 대체한다(예: `--text-1`이 없으면 `--text` 또는 `--text-2`). 사용한 토큰을 보고한다.

- [ ] **Step 6: Typecheck & build**

Run: `cd apps/renderer && npx tsc --noEmit && npm run build`
Expected: no errors, build success.

- [ ] **Step 7: Commit** (git 초기화 시)

```bash
git add apps/renderer/src/components/SchemaExplorer.tsx apps/renderer/src/App.css
git commit -m "feat(ddl): wire create-table menu + dialog into SchemaExplorer"
```

---

## Task 4: 전체 테스트 + 빌드 + CDP 실제 검증

**Files:** (없음 — 검증 전용)

- [ ] **Step 1: Unit tests + typecheck + build**

```bash
cd apps/renderer
export PATH="$HOME/.nvm/versions/node/v24.14.0/bin:$PATH"
npx vitest run        # ddlBuilder(buildCreateTable 포함) 전체 green
npx tsc --noEmit      # no errors
npm run build         # success
```

- [ ] **Step 2: 라이브 DB 확인**

```bash
docker ps --format '{{.Names}}' | grep -E 'dev-mysql|verify-pg'
```
없으면 verify-pg 기동(postgres:16, postgres/postgres). dev-mysql은 사용자 것 — 유지.

- [ ] **Step 3: CDP 실제 검증** (`--remote-debugging-port=9222` 인스턴스)

1. mysql 커넥션에서 **DB 노드 우클릭** → 메뉴에 "테이블 추가…" 노출 확인.
2. 클릭 → 다이얼로그 열림. 기본 `id BIGINT PK AI` 행 + 미리보기에 ``CREATE TABLE `...` ( `id` BIGINT NOT NULL AUTO_INCREMENT, PRIMARY KEY (`id`) )`` 형태 SQL 표시 확인.
3. 테이블 이름 `cdp_demo` 입력, 컬럼 `name VARCHAR(100) NOT NULL UNIQUE` 추가 → 미리보기 반영 확인.
4. 실행 → 성공 후 트리에 `cdp_demo` 등장 확인. `docker exec dev-mysql ... SHOW COLUMNS FROM devdb.cdp_demo`로 실제 생성 확인.
5. **편집-SQL 경로**: 다이얼로그를 다시 열고 SQL 텍스트영역을 직접 편집(예: 컬럼 한 줄 추가) → dirty 표시 확인 → 실행 → 반영 확인. [폼 기준으로 다시 생성] 클릭 시 폼 SQL로 복귀 확인.
6. postgres(verify-pg)에서도 1회 생성 → `GENERATED BY DEFAULT AS IDENTITY` SQL 확인 + 실행 성공.
7. **정리**: 검증용으로 만든 테이블은 DROP으로 제거(특히 `dev-mysql`은 사용자 DB — `cdp_demo` 등 반드시 삭제해 원상복구). `/tmp/*.mjs` 제거.

- [ ] **Step 4: 사용자 DB 원상복구 확인**

```bash
docker exec dev-mysql mysql -uroot -ppassword1! -e "SHOW TABLES FROM devdb;" 2>/dev/null
# devdb에는 원래의 demo_users만 남아야 한다.
```

---

## Self-Review (작성자 체크리스트 — 완료)

- **스펙 커버리지:** DB 노드 우클릭 메뉴(Task 3), 컬럼 폼+PK/AI/UNIQUE(Task 2), 편집 가능한 SQL+다시생성(Task 2), `buildCreateTable` 방언/PK/AI/UNIQUE/복합PK/기본값/PK NOT NULL 강제/AI default 무시/빈배열(Task 1), 트리 새로고침(Task 3, `refreshAfterDdl` 재사용), 정책 게이트 재사용(runDdl), 백엔드 무변경 — 모두 매핑됨.
- **플레이스홀더:** 없음(모든 코드 블록 완전).
- **타입 일관성:** `CreateColumnSpec`, `buildCreateTable`, `Driver`, `runDdl`, `refreshAfterDdl`, `dbMenu`/`create` 상태, `onApplied`/`onClose` 시그니처가 태스크 간 일치. CreateTableDialog는 `key={create.db}`로 마운트(상태 깔끔).
